interface WildMarketDetailPageProps {
  params: { id: string };
}

const WildMarketDetailPage = ({
  params: { id },
}: WildMarketDetailPageProps) => {
  return <div>WildMarketDetailPage</div>;
};

export default WildMarketDetailPage;
